import React from 'react';

// This component is no longer used after removing the login system.
// It is kept to avoid file deletion but is now effectively empty.
const ProfileScreen: React.FC = () => {
    return null;
};

export default ProfileScreen;